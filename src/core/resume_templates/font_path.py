from pathlib import Path

black = Path(f'src/core/resume_templates/Inter-Black.ttf').resolve()

bold = Path(f'src/core/resume_templates/Inter-Bold.ttf').resolve()

extra_bold = Path(f'src/core/resume_templates/Inter-ExtraBold.ttf').resolve()

extra_light = Path(f'src/core/resume_templates/Inter-ExtraLight.ttf').resolve()

light = Path(f'src/core/resume_templates/Inter-Light.ttf').resolve()

medium = Path(f'src/core/resume_templates/Inter-Medium.ttf').resolve()

regular = Path(f'src/core/resume_templates/Inter-Regular.ttf').resolve()

semi_bold = Path(f'src/core/resume_templates/Inter-SemiBold.ttf').resolve()

thin = Path(f'src/core/resume_templates/Inter-Thin.ttf').resolve()